<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:paddingVertical="11dp"
    android:paddingHorizontal="10dp"
    android:gravity="center_vertical"
    android:background="?attr/selectableItemBackground">

    <TextView
        android:id="@+id/editor_lyric_timestamp"
        android:layout_width="75dp"
        android:layout_height="wrap_content"
        android:text="00:00.00"
        android:textColor="#99FFFFFF"
        android:textSize="12sp"
        android:fontFamily="sans-serif-medium" />

    <TextView
        android:id="@+id/editor_lyric_text"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="5dp"
        android:layout_marginEnd="5dp"
        android:fontFamily="@font/montserrat_bold"
        android:text="Lyric Line Text"
        android:textColor="@color/white"
        android:textSize="18sp" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <ImageView
            android:id="@+id/editor_lyric_add_note"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:src="@drawable/ic_music_note"
            android:padding="4dp"
            android:contentDescription="Add Music Note"
            android:alpha="0.7"
            android:layout_marginEnd="4dp" />

        <ImageView
            android:id="@+id/editor_lyric_add_line"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:src="@android:drawable/ic_menu_add"
            android:padding="2dp"
            android:contentDescription="Add New Line"
            app:tint="@color/white"
            android:alpha="0.7" />
    </LinearLayout>

</LinearLayout>
